<p id="notice"><%= notice %></p>

<h1>Lugares</h1>

<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Ubicaci&oacute;n</th>
      <th>Descripci&oacute;n</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @places.each do |place| %>
      <tr>
        <td><%= place.Name %></td>
        <td><%= place.Location %></td>
        <td><%= place.Description %></td>
        <td><%= link_to 'Ver', place %></td>
        <td><%= link_to 'Editar', edit_place_path(place) %></td>
        <td><%= link_to 'Eliminar', place, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Crear Nuevo', new_place_path, class:'btn btn-outline-info' %>

<!--

<a href="#n()" class="btn btn-outline-info">HOLA</a>
<a href="#n()" class="btn btn-primary">HOLA</a>
<a href="#n()" class="btn btn-info">HOLA</a>
<a href="#n()" class="btn btn-success">HOLA</a>
<a href="#n()" class="btn btn-danger">HOLA</a>
<a href="#n()" class="btn btn-warning">HOLA</a>

-->