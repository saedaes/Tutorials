<p id="notice"><%= notice %></p>

<h1>Places</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Location</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @places.each do |place| %>
      <tr>
        <td><%= place.Name %></td>
        <td><%= place.Location %></td>
        <td><%= place.Description %></td>
        <td><%= link_to 'Show', place %></td>
        <td><%= link_to 'Edit', edit_place_path(place) %></td>
        <td><%= link_to 'Destroy', place, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Place', new_place_path, class:'btn btn-outline-info' %>


<a href="#n()" class="btn btn-primary">HOLA</a>
<a href="#n()" class="btn btn-info">HOLA</a>
<a href="#n()" class="btn btn-success">HOLA</a>
<a href="#n()" class="btn btn-danger">HOLA</a>
<a href="#n()" class="btn btn-warning">HOLA</a>
