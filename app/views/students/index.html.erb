<p id="notice"><%= notice %></p>

<h1>Alumnos</h1>

<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>Matr&iacute;cula</th>
      <th>Nombre</th>
      <th>Apellidos</th>
      <th>Celular</th>
      <th>Es Asesor</th>
      <th>Maestro Asesor Asignado</th>
      <th colspan="3" style="text-align:center;">Opciones</th>
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
      <tr>
        <td><%= student.Register %></td>
        <td><%= student.Name %></td>
        <td><%= student.LastName %> <%= student.SecondLastName %></td>
        <td><%= student.Cellphone %></td>
        <td><%= if student.IsAssessor then 'Sí' else 'No' end %></td>
        <td><%= student.TeacherAssessor %></td>
        <td style="text-align:center;"><%= link_to 'Ver', student %></td>
        <td style="text-align:center;"><%= link_to 'Editar', edit_student_path(student) %></td>
        <td style="text-align:center;"><%= link_to 'Eliminar', student, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Crear Alumno Nuevo', new_student_path, class:'btn btn-outline-info'  %>


